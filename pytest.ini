[pytest]
# El paquete vive en api/, no en la raiz. Esto evita tener que instalar el
# proyecto o pelear con PYTHONPATH.
pythonpath = api
testpaths = api/tests
markers =
    croma: llama a Croma de verdad (necesita CROMA_API_KEY)

# ---------------------------------------------------------------------------
# Lumen — plantilla de variables de entorno
#
# Copia este archivo a .env y pide los valores reales por el chat privado del
# equipo. .env esta en .gitignore: NUNCA lo commitees.
#
#   Windows:  Copy-Item .env.example .env
#   Mac/Linux: cp .env.example .env
# ---------------------------------------------------------------------------


# --- Croma (fuente unica de datos) -----------------------------------------
# API HTTP POST https://api.croma.run/co/.../v1. Ver HERRAMIENTAS.md y
# api/lumen/croma/client.py. El MCP de Cursor no lo usa el backend.
CROMA_API_KEY=


# --- IA (Cursor SDK) --------------------------------------------------------
# Presupuesto duro: US$50. Dueno: Freddy (B2). Revisar consumo a las 20:00 y 23:00.
CURSOR_API_KEY=
# Modelo por defecto para narracion y resolucion de entidades (rapido y barato).
LUMEN_MODELO_RAPIDO=composer-2.5
# Modelo fuerte, solo para el lector de justificaciones. Fijado 16.ago ~01:10
# corriendo Cursor.models.list() de verdad; bajado de claude-opus-5 a
# claude-sonnet-5 (~02:00) y de ahi a claude-haiku-4-5 (~02:50) por cuota del
# presupuesto de US$50 -- probado en vivo tras el cambio, sigue citando bien
# (ver docs/handoff/FREDDY-B2.md).
LUMEN_MODELO_FUERTE=claude-haiku-4-5
# Directorio de trabajo del agente. NUNCA la raiz del repo: un agente suelto
# sobre el repo lee archivos que nadie pidio y quema presupuesto.
LUMEN_SCRATCH_DIR=./scratch


# --- Supabase (base de datos y cache) --------------------------------------
# Un solo proyecto cloud compartido. Dueno unico de migraciones: Cristian (B3).
SUPABASE_URL=
SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
# Cadena de conexion directa, solo para la CLI de migraciones.
SUPABASE_DB_URL=


# --- Canal de alerta: Telegram o WhatsApp -----------------------------------
# Dueno: Freddy (B2). "telegram" o "whatsapp". Telegram es el canal de la
# demo desde las ~02:00 (Twilio trial exige plantillas aprobadas que la
# cuenta no tiene — ver docs/handoff/FREDDY-B2.md). WhatsApp sigue
# funcional en el repo, solo no se muestra en video.
LUMEN_CANAL_ALERTA=telegram

# Telegram: crear el bot con @BotFather -> /newbot, token al instante.
TELEGRAM_BOT_TOKEN=
# chat_id de quien recibe la alerta en el video. Se obtiene mandandole
# cualquier mensaje al bot y leyendo https://api.telegram.org/bot<TOKEN>/getUpdates
TELEGRAM_CHAT_ID_DEMO=

# --- WhatsApp (Twilio o Evolution API) --------------------------------------
# Vive en api/lumen/whatsapp/. Selecciona el proveedor real: "twilio" o
# "evolution".
LUMEN_WHATSAPP_PROVIDER=twilio

# Twilio (sandbox de WhatsApp)
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_WHATSAPP_FROM=
# Telefono que recibe la alerta en el video. En el sandbox tiene que haberse
# unido antes mandando "join <codigo>" al numero de Twilio.
TWILIO_WHATSAPP_TO_DEMO=

# Evolution API (alternativa a Twilio, si resuelve mejor)
EVOLUTION_API_URL=
EVOLUTION_API_KEY=
EVOLUTION_INSTANCE=
EVOLUTION_WHATSAPP_TO_DEMO=


# --- Aplicacion -------------------------------------------------------------
LUMEN_ENV=local
LUMEN_LOG_LEVEL=info

# Origenes permitidos por CORS, separados por coma. Andrew define su stack y su
# puerto: cuando lo sepa, se agrega aqui y en las variables de Render.
# El comodin "*" solo vale en local.
LUMEN_CORS_ORIGINS=*

# Dominio del frontend de Andrew (para el enlace dentro del WhatsApp, no para
# CORS). Vacio hasta que lo declare.
LUMEN_FRONTEND_URL=

# Respaldo de grabacion (§5.3). En true, la API responde desde el dump JSON
# versionado en vez de tocar Supabase o Croma. Existe para que un corte de red
# a las 07:00 no cueste el hackathon. NO es la arquitectura.
LUMEN_USAR_DUMP_LOCAL=false
LUMEN_DUMP_PATH=./fixtures/casos_demo.json

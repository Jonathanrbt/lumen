# Dependencias del backend de Lumen.
#
# Versiones fijadas a proposito: los cuatro tenemos que tener exactamente lo
# mismo, y a las tres de la manana nadie quiere depurar una diferencia de minor.
#
# Para anadir una: ESCRIBELA AL FINAL DEL ARCHIVO, nunca en medio. Asi git
# resuelve casi todos los conflictos solo cuando dos personas anaden a la vez.
# Y avisa en el chat del equipo para que los demas reinstalen.
#
#   python -m venv .venv
#   .\.venv\Scripts\Activate.ps1     (Mac/Linux: source .venv/bin/activate)
#   pip install -r api/requirements.txt
#
# Probado sobre Python 3.13.5.

fastapi==0.141.1
uvicorn[standard]==0.52.3
pydantic==2.13.4
pydantic-settings==2.15.0
httpx==0.28.1
python-multipart==0.0.32

# Cliente de Supabase. Dueno: Cristian (B3).
supabase==2.31.0

# Pruebas de humo del contrato: valida que los fixtures cuadren con los modelos.
pytest==9.1.1

# --- Anade aqui abajo ---

# Twilio: envio de alertas por WhatsApp sandbox. Dueno: Cristian (B3).
twilio==9.11.0

# Zonas IANA en Windows. Sin esto ZoneInfo("America/Bogota") de Cristian
# tumba cualquier import de la app (incluido POST /analizar).
tzdata==2025.2

# SDK de Cursor: llm_client, lector de justificaciones, narracion. Dueno:
# Freddy (B2). Nombre confirmado instalandolo, no a ciegas: `cursor-sdk`.
# Requiere Python >=3.10.
cursor-sdk==1.0.28

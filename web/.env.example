# URL base de la API. Sin esto la app corre entera contra fixtures/.
# Local:  http://127.0.0.1:8000
# Render: la que publique Cristian (B3) antes de las 19:00.
#
# Al desplegar, avisarle el dominio del frontend para que entre en
# LUMEN_CORS_ORIGINS. Si esto se descubre a las 02:00 se pierden treinta minutos.
VITE_API_URL=
